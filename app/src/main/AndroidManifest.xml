<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yupi.home.panikerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.SignUpActivity" />
        <activity android:name=".ui.CommentsActivity" />
        <activity android:name=".ui.MessagesActivity" />
        <activity android:name=".ui.WaitTimeActivity" />
        <activity android:name=".ui.DefaultRecipientsActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.SignUpFinishActivity" />
        <activity android:name=".ui.InsidePostActivity" />
        <activity android:name=".ui.PhoneCallActivity" />
        <activity android:name=".ui.VideoCallActivity"></activity>
    </application>

</manifest>